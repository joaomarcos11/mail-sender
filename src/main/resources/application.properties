# SQS Configuration
sqs.queue-url=https://sqs.us-east-1.amazonaws.com/520576585750/framer-notification
sqs.region=us-east-1

# AWS Credentials (use IAM roles in production)
quarkus.sqs.sync-client.type=aws-crt
# quarkus.sqs.aws.credentials.static-provider.access-key-id=${AWS_ACCESS_KEY}
# quarkus.sqs.aws.credentials.static-provider.secret-access-key=${AWS_SECRET_KEY}

# SMTP Configuration (Outlook)
quarkus.mailer.auth-methods=DIGEST-MD5 CRAM-SHA256 CRAM-SHA1 CRAM-MD5 PLAIN LOGIN
quarkus.mailer.from=fiapx.video.notification@gmail.com
quarkus.mailer.host=smtp.gmail.com
quarkus.mailer.port=587
quarkus.mailer.start-tls=REQUIRED
quarkus.mailer.username=fiapx.video.notification@gmail.com
quarkus.mailer.password="nfvq\ zrhr\ ezhq\ xdfe"

# quarkus.mailer.from=fiapx.video.notification@gmail.com

# Ensure "from" matches your Outlook email
# quarkus.mailer.from=fiap44@outlook.com
# quarkus.mailer.ssl=false
# quakurs.mailer.start-tls=true

# Quarkus Scheduler Configuration
quarkus.scheduler.enabled=true

# quarkus.log.category."io.quarkus.mailer".level=DEBUG
